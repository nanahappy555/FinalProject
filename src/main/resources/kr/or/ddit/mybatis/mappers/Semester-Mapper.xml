<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Semester-Mapper">
	<select id="selectAvailableSemester" resultType="semester">
		select
		*
		from semester
		where available='1'
	</select>

</mapper>